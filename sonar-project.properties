# Required metadata
sonar.projectKey=org.sonarqube:svg-polygon-decorator
sonar.projectName=JavaScript :: Svg Polygon Decorator
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=js

# Encoding of sources files
sonar.sourceEncoding=UTF-8
